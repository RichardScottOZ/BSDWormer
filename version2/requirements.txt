# Core dependencies
numpy>=1.20.0,<2.0.0
scipy>=1.7.0
matplotlib>=3.3.0
networkx>=2.5

# Geospatial dependencies
# Note: GDAL should be installed via system package manager first
# Ubuntu/Debian: sudo apt-get install python3-gdal
# macOS: brew install gdal
# Then install Python bindings matching your GDAL version:
GDAL>=3.0.0

# Optional: PyYAML for configuration files
PyYAML>=5.4.0
